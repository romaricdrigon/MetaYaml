_root:
     # _root is by default an array
     # and is required
    _content:
        fleurs:
            _metadata:
                _type: array
                _required: true
            _content:
                rose:
                    _metadata:
                        _required: true
                        _type: text
                violette:
                    _metadata:
                        _type: text
